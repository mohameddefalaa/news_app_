import 'package:flutter/material.dart';

class Seetings extends StatelessWidget {
  const Seetings({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Seetings"));
  }
}
